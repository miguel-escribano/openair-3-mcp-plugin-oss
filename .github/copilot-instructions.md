# OpenAir MCP — VS Code / Copilot

Read `agents/openair-agent.md`. Wire MCP via `.vscode/mcp.json` — see `.mcp.json.example`.
Prefer `ingest-local-export` for local files on a remote MCP host. No LLM parsing, no hand-built JSON.
